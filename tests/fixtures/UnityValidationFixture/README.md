# Unity Validation Fixture

Minimal Unity project used to verify the harness batch validation path.

- Unity Editor: `6000.4.10f1`
- EditMode: pure C# behavior
- PlayMode: `MonoBehaviour` integration

Generated directories such as `Library`, `Logs`, `Temp`, and `UserSettings` are
not version controlled.
