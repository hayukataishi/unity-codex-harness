# 共通Script・System設計

Scene固有ではないSystem、Service、Component、Assembly単位で`<system-key>.md`を
作成します。責務、public contract、依存方向、lifecycle、threading、error handlingを
記録し、class一覧だけの文書にはしません。

各Approved設計項目は`**上流AC:**`と`実装マッピング`を持ちます。
